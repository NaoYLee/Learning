var x = 1;  // 每个语句以;结尾
var y = 2; var z = 3;  // 不建议一行写多个语句
// 语句块是一组语句的集合
if (2 > 1) {
    x = 1;
    y = 2;
    z = 3;
    if (x < y) {
        z = 4;
    }  // 语句块可以嵌套使用
    if (x > y) {
        z =5;
    }
}
// 这是注释
/* 这是
多行
注释 */
// JavaScript严格区分大小写